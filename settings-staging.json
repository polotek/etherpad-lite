/*
  This file must be valid JSON. But comments are allowed
*/

{
  //Ip and port which etherpad should bind at
  "ip": "0.0.0.0",
  "port" : 9001,
  "env"  : "staging",

  //The Type of the database. You can choose between sqlite and mysql
  "dbType" : "pgsql",
  "dbSettings" : {
                    "user"    : "paddie",
                    "host"    : "stagepdb-001",
                    "password": "pa44i3",
                    "database": "yam_pages"
                  },

  //the default text of a pad
  "defaultPadText" : "",

  /* if true, all css & js will be minified before sending to the client. This will improve the loading performance massivly,
     but makes it impossible to debug the javascript/css */
  "minify" : true,

  /* This is the path to the Abiword executable. Setting it to null, disables abiword.
     Abiword is needed to enable the import/export of pads*/
  "abiword" : null,

  /* The log level we are using, can be: DEBUG, INFO, WARN, ERROR */
  "loglevel": "DEBUG",

  "logDirectory": "/var/log/paddie",

  "archiveDirectory": "/var/log/paddie/archives"

  , "tokie": {
        "host":"stagetokie-004.sjc1.yammer.com",
        "port":7080
  }

  , "workfeed": {
    "host": "https:\/\/stageworkfeed.sjc1.yammer.com"
    , "pathPrefix": "/api/v1"
  }
}

